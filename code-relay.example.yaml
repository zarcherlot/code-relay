# Code Relay host configuration. The plugin/MCP tools remain the normal entry point.

role: orchestrator
runtime: go
runner_label: codex-b
repository_root: .

# B host uses:
# role: checkpoint
# runtime: go
# runner_label: codex-b
# Set CODE_RELAY_INVITE_SECRET on both A and B when signed invitation integrity
# is required (32+ characters).
