# Code Relay host configuration. The plugin/MCP tools remain the normal entry point.

role: orchestrator
runtime: go
runner_label: code-relay-checkpoint
repository_root: .

# Checkpoint host uses:
# role: checkpoint
# runtime: go
# runner_label: code-relay-checkpoint
# Set CODE_RELAY_INVITE_SECRET on both Dev Host and Checkpoint Host when signed invitation integrity
# is required (32+ characters).
