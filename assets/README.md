# Brand assets

`icon.png` is the final approved Code Relay icon and the source of truth for README and release artwork.

`overview.png` is the canonical workflow diagram used in the project README. It depicts the task flow from the development host through Relay to the target host, followed by the receipt returned to the development host.
