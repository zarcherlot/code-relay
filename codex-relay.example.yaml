# Legacy config name; new installations should use code-relay.example.yaml. The
# Code Relay plugin/MCP tools remain the
# normal user entry point. CLI flags are provided for developer diagnostics.

# A host
role: orchestrator
poll_interval_seconds: 5
repository_root: .

# B host uses:
# role: verifier
# Set CODE_RELAY_INVITE_SECRET in the host environment on both A and B when
# signed invitation integrity is required (32+ characters).
