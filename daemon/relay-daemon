#!/usr/bin/env python3
"""Launcher for the relay daemon (use `python daemon/relay-daemon ...` on Windows)."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from codex_mate.daemon import main

if __name__ == "__main__":
    raise SystemExit(main())
